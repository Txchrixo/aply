/**
 * LLM Provider abstraction.
 * See types.ts for the interface.
 */
export type { LLMProvider, ProviderConfig, Language, CoverLetterInput, CoverLetterResult, FormQuestion, FormAnswer, JobExtraction } from "./types";
export { createLLMProvider } from "./types";
